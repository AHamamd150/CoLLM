Generate a complete Python analysis script for Delphes ROOT files based on these specifications:

## SELECTION CUTS
{selection_cuts}

## PLOTS FOR VALIDATION
{plots_for_validation}

## OUTPUT STRUCTURE
{output_structure}

## INSTRUCTIONS
1. Output ONLY valid Python code below (no markdown, no explanations)
2. The code must be complete and runnable as: python script.py input.root
3. Do NOT use placeholder syntax like {{branch}} or {{count}} - use real variable names
4. Include vector.register_awkward() after imports
5. Use tree.arrays(..., library="ak") to load data
6. Save all plots with plt.savefig(..., dpi=150) followed by plt.close()

## OUTPUT CODE (Python only, starting with imports):